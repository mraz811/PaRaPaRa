<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="para.pao">
 	
 	<resultMap id="ItemDto" type="ItemDto">
		<result property="item_name" column="ITEM_NAME" />
		<result property="item_price" column="ITEM_PRICE" />
	</resultMap>
 	
	<resultMap id="StockDto" type="StockDto">
		<result property="stock_seq" column="STOCK_SEQ" />
		<result property="store_code" column="STORE_CODE" />
		<result property="stock_name" column="STOCK_NAME" />
		<result property="stock_qty" column="STOCK_QTY" />
		<association property="itemDto" resultMap="itemDto"/>
	</resultMap>

	
	<!-- 업주 : 매장 발주 내역 조회 -->
	<select id="paoList" parameterType="java.lang.String" resultType="PaoInfoDto">
	SELECT PAO_SEQ, STORE_CODE, STORE_NAME, PS_NAME, PAO_DATE
	FROM PAOINFO
	WHERE STORE_CODE = #{store_code}
	</select>
	
	<!-- 업주 : 매장 발주 상태 선택 조회 -->
	<select id="paoSelectStatus" parameterType="java.util.Map" resultType="PaoInfoDto">
	SELECT PAO_SEQ, STORE_CODE, STORE_NAME, PS_NAME, PAO_DATE
	FROM PAOINFO
	WHERE STORE_CODE = #{store_code} AND
		PS_CODE IN
		<foreach collection="status_list" item="status" separator="," open="(" close=")">
			#{status}
		</foreach>
	</select>
	
	<!-- 업주 : 매장 발주 날짜 선택 조회 -->
	<select id="paoSelectDate" parameterType="java.util.Map" resultType="paoInfoDto">
	SELECT PAO_SEQ, STORE_CODE, STORE_NAME, PS_NAME, PAO_DATE
	FROM PAOINFO
	WHERE STORE_CODE = #{store_code} AND
		<![CDATA[
        	PAO_DATE >= #{startDate} AND PAO_DATE <= #{endDate}
    	]]>
	</select>
	
	
	
	<!-- 업주 : 발주 신청 시 재고 목록 조회 -->
	<!-- 
	<select id="paoStockList" parameterType="java.lang.String" resultType="StockDto">
	SELECT STOCK_SEQ, STORE_CODE, STOCK_NAME, NVL(STOCK_QTY, 0), ITEM_PRICE
	FROM STOCK JOIN ITEM ON STOCK.STOCK_NAME = ITEM.ITEM_NAME
	WHERE STORE_CODE = #{store_code}
	ORDER BY STOCK_NAME
	</select>
	 -->
	
</mapper>
